---
title: Minimum Number of Vertices to Reach All Nodes
category: Graphs
difficulty: Medium
---

<Callout>

[Leetcode Link](https://leetcode.com/problems/minimum-number-of-vertices-to-reach-all-nodes)

</Callout>

## Description

Given a directed acyclic graph, with <code>n</code> vertices numbered from <code>0</code> to <code>n-1</code>, and an array <code>edges</code> where <code>edges[i] = [from<sub>i</sub>, to<sub>i</sub>]</code> represents a directed edge from node <code>from<sub>i</sub></code> to node <code>to<sub>i</sub></code>.

Find _the smallest set of vertices from which all nodes in the graph are reachable_. It&#39;s guaranteed that a unique solution exists.

Notice that you can return the vertices in any order.

Example 1:

<Image
	alt=""
	src="https://assets.leetcode.com/uploads/2020/07/07/untitled22.png"
	width={231}
	height={181}
/>

<br />
<pre className="overflow-x-auto">
	Input: n = 6, edges = [[0,1],[0,2],[2,5],[3,4],[4,2]] Output: [0,3]
	<b>Explanation: </b>It&#39;s not possible to reach all the nodes from a single
	vertex. From 0 we can reach [0,1,2,5]. From 3 we can reach [3,4,2,5]. So we output
	[0,3].
</pre>

Example 2:

<Image
	alt=""
	src="https://assets.leetcode.com/uploads/2020/07/07/untitled.png"
	width={201}
	height={201}
/>

<br />
<pre className="overflow-x-auto">
	Input: n = 5, edges = [[0,1],[2,1],[3,1],[1,4],[2,4]] Output: [0,2,3]
	Explanation: Notice that vertices 0, 3 and 2 are not reachable from any other
	node, so we must include them. Also any of these vertices can reach nodes 1
	and 4.
</pre>

Constraints:

    - <code>2 &lt;= n &lt;= 10^5</code>
    - <code>1 &lt;= edges.length &lt;= min(10^5, n * (n - 1) / 2)</code>
    - <code>edges[i].length == 2</code>
    - <code>0 &lt;= from<sub>i,</sub> to<sub>i</sub> &lt; n</code>
    - All pairs <code>(from<sub>i</sub>, to<sub>i</sub>)</code> are distinct.

## Solution

### Javascript

```javascript
Solution will be updated soon
```

### Python

```python
Solution will be updated soon
```

### C++

```cpp
Solution will be updated soon
```

### Java

```java
class Solution {
    public List<Integer> findSmallestSetOfVertices(int n, List<List<Integer>> edges) {
        int[] incoming = new int[n];
        for(List<Integer> ls: edges){
            int in = ls.get(1);
            incoming[in]++;
        }
        List<Integer> res = new ArrayList<>();
        for(int i=0; i<n; i++){
            if(incoming[i] == 0)
                res.add(i);
        }
        return res;
    }
}

```
